/**
 * Domain layer — pure Java business model.
 *
 * <p>No Spring, JPA, Spring AI, Jackson, or Lombok dependencies are allowed here. Bounded
 * contexts live in sub-packages (user, agent, conversation, tool, mcp, ratelimit, auth).
 */
package com.cognizant.emk.multiagent.domain;
