/**
 * Cross-cutting application primitives, e.g. the injected {@code Clock} port. Populated
 * incrementally as feature EPICs need shared abstractions.
 */
package com.cognizant.emk.multiagent.application.shared;
