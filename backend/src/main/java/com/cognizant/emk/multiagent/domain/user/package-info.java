/**
 * User bounded context — domain layer.
 *
 * <p>Carries the {@code User} aggregate, its value objects (UserId, Email, Password, Role),
 * the {@code UserRepository} interface, and per-context business exceptions. Populated by
 * EPIC-05.
 */
package com.cognizant.emk.multiagent.domain.user;
