/**
 * REST adapter root. One sub-package per bounded context (auth, admin, agent, conversation,
 * catalog) plus shared concerns (error, pagination, security, ratelimit).
 */
package com.cognizant.emk.multiagent.infrastructure.web;
