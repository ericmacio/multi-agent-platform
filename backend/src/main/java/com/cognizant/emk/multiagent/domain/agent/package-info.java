/**
 * Agent bounded context — domain layer.
 *
 * <p>Carries the {@code Agent} aggregate, value objects (AgentId, AgentConfig, MemorySize, Team),
 * the {@code AgentRepository} interface, and per-context business exceptions. Populated by
 * EPIC-06.
 */
package com.cognizant.emk.multiagent.domain.agent;
