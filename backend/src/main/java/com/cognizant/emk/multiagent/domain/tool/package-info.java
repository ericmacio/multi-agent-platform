/**
 * Tool bounded context — domain layer.
 *
 * <p>Carries {@code ToolDescriptor} and the per-context business exceptions
 * (e.g. {@code UnknownToolException}). Populated by EPIC-07.
 */
package com.cognizant.emk.multiagent.domain.tool;
