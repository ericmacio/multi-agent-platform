/**
 * Auth bounded context — domain layer.
 *
 * <p>Carries {@code ApiKey}, {@code ClientId}, the sealed {@code Principal} type
 * (UserPrincipal | SystemPrincipal), and {@code ApiKeyRepository}. Populated by EPIC-04.
 */
package com.cognizant.emk.multiagent.domain.auth;
