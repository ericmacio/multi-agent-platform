/**
 * Infrastructure layer — adapters that implement domain or application ports.
 *
 * <p>Holds the REST controllers ({@code web}), persistence adapters ({@code persistence}),
 * security filters and configuration ({@code web/security}, {@code security}), the LLM
 * adapter ({@code llm/openai}), the tool catalog adapter ({@code tool}), the MCP adapter
 * ({@code mcp}), and the application configuration ({@code config}).
 */
package com.cognizant.emk.multiagent.infrastructure;
