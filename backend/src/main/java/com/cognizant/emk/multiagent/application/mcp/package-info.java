/**
 * MCP bounded context — application layer. {@code McpServerCatalog} port plus the
 * enumeration use case and its {@code McpServerDescriptor} wire shape.
 * {@code FilesystemMcpUserScope} (US-08-004) lives in this package once it lands.
 */
package com.cognizant.emk.multiagent.application.mcp;
