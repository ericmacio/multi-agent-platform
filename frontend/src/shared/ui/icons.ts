/**
 * Curated re-exports from `lucide-react`. Keeping the list explicit prevents
 * the UI vocabulary from sprawling and keeps the bundle slim — feature EPICs
 * extend it via PR rather than `import` arbitrarily.
 */
export {
  AlertTriangle,
  Bot,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronRight,
  Copy,
  Eye,
  EyeOff,
  Info,
  Key,
  LayoutDashboard,
  LoaderCircle,
  LogOut,
  MessageSquare,
  MoreHorizontal,
  Pencil,
  Plus,
  Search,
  Send,
  Server,
  Settings,
  Square,
  Trash2,
  User,
  Users,
  Wrench,
  X,
  XCircle,
} from 'lucide-react';
export type { LucideIcon, LucideProps } from 'lucide-react';
