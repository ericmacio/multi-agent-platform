/**
 * Test-data factories that build payloads matching the openapi schemas.
 *
 * Empty for EPIC-01. Feature EPICs populate this file with helpers like
 * `agent(overrides?: Partial<Agent>): Agent` so individual tests can describe
 * intent ("an agent named X") without restating every required field.
 */
export {};
